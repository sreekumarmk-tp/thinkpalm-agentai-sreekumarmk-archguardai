# pytest.ini
[pytest]
# So `import src...` works from any CWD (e.g. IDE running tests from tests/e2e/)
pythonpath = .
filterwarnings =
    ignore:.*create_react_agent has been moved.*:DeprecationWarning
    ignore:.*LangGraphDeprecatedSinceV10.*